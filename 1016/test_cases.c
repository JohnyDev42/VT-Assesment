###Test Cases######


1.Inserted nodes at end of the list

2.Printed the list using head node

3.swap:
	swap head node with next 
	swap any middle node with next node
	if last node is passed it wont swap with next node

4.delete:
	delete head node
	delete any middle node
	delete last node

5.check the next_next value every time
